/*
const db = require('../config/db');

class Post {
    constructor(devicename, post) {
        this.devicename = devicename;
        this.body = body;
    }

    async save(){
        let d = new Date();
        let yyyy = d.getFullYear;
        let mm = d.getMonth()+1;
        let dd = d.getDate();

        let createdAtDate = `${yyyy}-${mm}-${dd}`;

        let sql = `
        INSERT INTO device(
            status,
            details,
            category,
            name,
            ruleExt,
            ruleDur,
            qrCode, 
            storage, 
            launchYr, 
            cost
        )
        VALUES(
            'Available',
            ${},
            ${},

        )
        `

    }
    static findAll() {

    }
}
*/
